# 变更记录

[English](./CHANGELOG.md) · **简体中文**

本文记录 CherryChat 的可追溯发布版本。英文版本是 Release notes 的基准；简体中文
版本必须保持相同的能力与限制边界。

## [Unreleased]

## [1.2.1] - 2026-08-31

### 摘要

`v1.2.1` 是补丁发布，修复一个 Hosted 认证状态缺陷：使用已被替换的访问码发起的
延迟请求，可能在新访问码已经验证成功之后，把浏览器重新标记为未认证。

### 修复

- Hosted 认证状态改为由发起请求的那一次持有。模型列表刷新、聊天生成、图片生成和
  自动标题请求各自绑定发起时的认证 epoch，只有该 epoch 仍然有效时才能把浏览器报告
  为未认证。在新访问码验证成功之后才返回的旧访问码拒绝结果，不再清除新的验证结果。
- 收到 Hosted 认证错误的自动标题请求现在会更新认证状态。此前该错误被直接丢弃，
  因此访问码确实被撤销时这条链路不会给出任何信号。标题生成仍然是静默的非关键增强，
  自身依旧不报错。
- Hosted 图片生成改为在请求前紧邻处同时读取访问码并绑定 epoch。参考图加载是异步的，
  原先的顺序可能让一次请求绑定到用户已经替换掉的访问码上，从而丢弃真实的拒绝结果。
- 切换到 BYOK 之后才返回的 Hosted 认证错误不再改变 Hosted 认证状态。
- Hosted 网络搜索只把 Hosted 专用的拒绝视为访问码失效。同源检查的 `FORBIDDEN` 和
  部署自身上游凭据的 `UNAUTHORIZED` 仍然让搜索调用失败，但不会报告访问码无效。

认证失败仍然保留本地保存的访问码，用户可以在设置页重新验证，不需要重新输入。上游
与 BYOK 的 401、429、5xx、超时和取消继续与 Hosted 认证状态隔离：只有 CherryChat
自身返回的 `HOSTED_AUTH_REQUIRED` 与 `ACCESS_CODE_INVALID` 会改变认证状态。

### 已知限制

- 自动标题链路与其他请求路径共用同一套认证状态投影，但没有专门的回归测试；其行为
  通过模型刷新与网络搜索两条路径间接覆盖。
- `v1.2.0` 列出的全部限制依然适用。本次发布不改变认证协议、请求头、服务端配置或
  已存储的数据。

### 升级与备份

本次发布不新增 IndexedDB 结构迁移，也不需要修改配置。本地对话、设置和已保存的访问码
全部保留，部署侧无需更新任何凭据。

由于 `v1.2.0` 已经包含更新提示，长时间打开的页面可以自行提示升级到本版本。

## [1.2.0] - 2026-08-30

### 摘要

`v1.2.0` 在 `v1.1.0` 建立的浏览器本地、BYOK、Hosted access、搜索、图片生成、
备份和部署契约上，将 Hosted 认证改为无状态校验，并增加原图预览与下载。

### 主要能力

- Hosted access 改为每个请求都对照当前 `ACCESS_CODE` 白名单校验。浏览器通过
  `X-CherryChat-Access-Code` 请求头重新提交本地保存的访问码，因此仍然有效的
  访问码不再受 7 天会话限制，可以跨 Cookie 过期、重新部署和域名变更继续使用。
- 缺少请求头报告 `HOSTED_AUTH_REQUIRED`，访问码被拒报告 `ACCESS_CODE_INVALID`，
  两者都是 HTTP 401，因此 Hosted 认证失败可以与上游和 BYOK API Key 失败区分。
- 生成的图片可以打开无障碍的原始尺寸预览，并保存为带时间戳的本地下载文件。
- 关于页面报告当前运行的部署版本；长时间打开的页面在有更新版本发布后会提示
  刷新，刷新只重新加载文档，本地会话与设置都会保留。

### 变更

- 移除 Hosted 会话的签发与校验。`POST /api/auth` 仅用于设置页显式验证访问码并
  返回布尔结果；`DELETE /api/auth` 仅清理早期版本留下的旧
  `cherrychat_session` Cookie。
- **撤销 Hosted access 现在等同于修改 `ACCESS_CODE`。** 移除或替换某个访问码会
  在该访问码的下一次请求生效。轮换 `AUTH_SECRET` 不再使客户端登出，它只改变
  摘要派生方式和限流客户端指纹。
- 访问码请求头改为百分号编码，因为 HTTP 请求头无法承载配置允许的非 ASCII
  访问码。编码格式错误按无效访问码处理，而不是按缺失处理，因此无法绕过限流。
- 任意 Hosted 路由上被拒的访问码，现在都会计入保护显式登录的同一套单客户端与
  全局失败窗口，被限流的客户端会收到带 `Retry-After` 的 HTTP 429。
  `GET /api/config` 是唯一例外：它不会因认证失败而失败，被限流时报告
  `authenticated: false`，因此页面仍可加载，该 Endpoint 也不会成为爆破探针。
- 允许重新提交未修改的 Hosted 访问码，设置页的改动检测不再阻止重新验证。
- 修复启动顺序缺陷：`GET /api/config` 曾在本地连接解析完成之前发出，导致有效的
  已保存访问码被读为未认证。

### 已知限制

- 访问码请求头只会附加到同源 CherryChat 路由，绝不会转发给上游模型、搜索或
  图片服务。它是部署方共享的凭据，不是按用户区分的账号：CherryChat 仍不提供
  账号、云同步、全局用量账本、计费控制或托管服务 SLA。
- 认证失败与并发防护都是进程内状态。在 Serverless 平台上它们不构成全局一致的
  配额。公网 Hosted 部署仍然需要上游消费限额和滥用应对方案。
- 刷新提示只比较 `major.minor.patch`，忽略预发布元数据。它在页面重新获得焦点或
  可见性时检查，而不是定时轮询，因此从不失去焦点的页面不会发现新部署。

### 升级与备份

本次发布不引入 IndexedDB 结构迁移，本地会话、设置和已保存的访问码都会保留。

仍在运行 `v1.2.0` 之前版本的浏览器不会发送访问码请求头，其 Hosted 请求会收到
`HOSTED_AUTH_REQUIRED`，直到重新加载页面。重新加载即可恢复，不需要重新输入
访问码。由于刷新提示本身随本次发布引入，在此之前加载的页面无法为自己显示该
提示。部署方在更新后应预期需要手动刷新一次长时间打开的标签页。

在更新既有部署或浏览器配置之前请创建完整备份；备份仍然有意不包含凭据。

### 部署与安全

本次发布不需要新的部署凭据，既有 `ACCESS_CODE` 继续有效。此前依赖轮换
`AUTH_SECRET` 来让客户端登出的部署方，现在必须移除或替换共享的 `ACCESS_CODE`。
访问码会被规范化、限制在 256 UTF-8 字节内、经 HMAC-SHA-256 摘要后与每一个已
配置访问码做常量时间比较；访问码不会写入响应、错误详情或日志。

## [1.1.0] - 2026-08-18

### 摘要

`v1.1.0` 在 `v1.0.0` 建立的浏览器本地、BYOK、Hosted access、搜索、备份和
部署稳定契约上，增加了集成图片生成与参考图编辑能力。

### 主要能力

- 新增独立图片生成模式，内置 `gpt-image-2` BYOK 连接，并提供分辨率、宽高比、
  质量、格式和压缩控制。
- 支持通过兼容的 OpenAI 风格生成/编辑 Endpoint，根据提示词或最多 16 张有序
  参考图生成图片。
- 新增由部署方付费的 Hosted 图片生成，可使用单组旧版配置或服务端 Profile 白名单，
  且不会向访问码用户暴露上游凭据或 URL。
- 图片生成消息会保存生成快照与本地生成图片附件，并支持 Backup v2 往返恢复以及
  JSON/Markdown 导出。

### 变更

- 浏览器 BYOK 图片设置简化为一个服务 URL 和 API Key，模型固定为 `gpt-image-2`；
  多 Profile 仍仅属于 Hosted 部署能力。
- 图片服务 Root 与 `/v1` Base 会规范化为标准 `/v1/images/generations` 和
  `/v1/images/edits` Endpoint，并增加有界图片响应和更严格的 Hosted URL 校验。
- 关于页面改为从 Package 元数据读取应用版本并提供仓库链接；同时优化用户消息
  展示和行内编辑。
- 本地开发改用 Webpack，以提高 Windows 上的启动稳定性。

### 已知限制

- CherryChat 不提供图片生成额度或 Provider 服务。BYOK 用户和部署者需要自行承担
  Provider 可用性、费用、限流和内容政策合规责任。
- 浏览器直连图片生成要求 Provider 支持 CORS。兼容性取决于所配置服务是否支持
  文档约定的 OpenAI 风格 Endpoint 与参数；兼容服务可能只实现其中一部分。
- 生成图片保存在当前浏览器中，可能显著增加 IndexedDB 和 Backup v2 体积。
  CherryChat 仍不提供账号、云同步、全局用量账本、计费控制或托管服务 SLA。

### 升级与备份

本版本没有新增 IndexedDB Schema 迁移。Backup v2 现在可以往返保存生成图片和有序
参考图关系。更新现有部署或浏览器 Profile 前请先创建完整备份；凭据仍会被有意排除。

### 部署与安全

BYOK-only 图片生成不需要部署端凭据。Hosted 图片生成要求完整的 Hosted access
配置，并额外提供一组旧版图片配置或 Profile 白名单。向其他用户开放前，应独立验证
固定上游、消费限额、Firewall 策略、Function 日志和真实生成/编辑行为；仓库 CI
不能证明这些部署属性。

## [1.0.0] - 2026-08-12

### 摘要

`v1.0.0` 是 CherryChat 首个正式稳定版本。CherryChat 是轻量、隐私优先、可自托管
的 Web AI 对话客户端。本版本稳定了文档内的浏览器本地数据、BYOK、Hosted access、
搜索、备份和部署契约，同时保留下列限制。

### 主要能力

- 浏览器本地对话、分支、附件、助手、本地搜索、打印和导入导出工作流。
- 支持 OpenAI Chat Completions、OpenAI Responses、原生 Anthropic、原生 Gemini、
  New API Endpoint Metadata 和通用 OpenAI-compatible Chat Endpoint 的 BYOK 连接。
- 模型感知推理控制、流式答案与推理展示，以及图片输入。
- Tavily、Exa 和 Grok 网络搜索。Grok 默认使用 `grok-4.5`，并支持由部署者配置兼容
  URL、模型和可选的 X Search。
- 可选的部署方 Hosted access，通过固定上游、访问码、签名 HttpOnly Session、模型
  与搜索 Provider 白名单和有界服务端路由提供能力。Hosted 用户可以在已配置的搜索
  Provider 中选择，但不会收到 Provider 凭据或服务端专属选项。
- Backup v2、经过校验的导入，以及不含凭据的导出。
- 格式、文档、Lint、严格 TypeScript、覆盖率、脚本、生产构建、依赖审计、客户端
  Secret 扫描，以及 Chromium 桌面端和移动端浏览器质量门。

### 已知限制

- 不提供账号、云同步、组织角色、集中审计、全局用量账本、计费控制或企业支持承诺。
- 浏览器保存的 BYOK 凭据只是便利存储，并非加密密码库；对话和设置保留在当前
  浏览器中。
- Hosted 模型访问只支持部署固定的 OpenAI-compatible Chat Completions 路径。原生
  Provider、OpenAI Responses 和 New API Endpoint Routing 仍属于 BYOK 能力。
- Hosted 搜索 Provider、凭据、URL、Grok 模型和 X Search 行为由部署者配置；浏览器
  只能选择服务端白名单中的 Provider。
- 仓库 CI 通过不能证明某个 Vercel 域名、环境变量、Firewall 规则、消费限额或上游
  Provider 已正确配置。
- 不提供安全响应 SLA、漏洞奖励、长期支持分支或旧版本维护时间保证。

### 升级与备份

本版本不引入新的数据 Schema 迁移。更新部署或浏览器安装前，请创建完整 Backup v2
并保存在浏览器 Profile 之外。备份与普通导出有意排除 API Key、访问码、Cookie 和
凭据摘要。

向已有浏览器 Profile 导入前，请阅读[数据与备份行为](./docs/DATA_CN.md)。

### 部署与安全

BYOK-only 部署不需要在 Vercel 中配置 Provider 凭据。Hosted access 必须使用完整
服务端配置，并由部署者设置 Firewall 与消费控制。Vercel Production Alias 不代表
项目承诺托管服务可用性、安全响应时限或企业支持。

发布实例或版本前，请阅读[部署指南](./docs/DEPLOYMENT_CN.md)、
[安全策略](./docs/SECURITY_CN.md)和[发布政策](./docs/RELEASES_CN.md)。
